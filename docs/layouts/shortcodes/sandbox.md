### Testing an Example

The examples on this page can be run in the site's code sandbox. To copy and paste an example from above, mouse over its box and click the `copy` button 
that appears in the upper right corner.

You can then [visit the code sandbox](../../../sandbox/) to paste and run the code.
