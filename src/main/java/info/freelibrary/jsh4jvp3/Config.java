
package info.freelibrary.jsh4jvp3;

/**
 * The application's configuration variables.
 */
final class Config {

    /** The HTTP port configuration constant. */
    static final String HTTP_PORT = "HTTP_PORT";

    /** The maximum request size configuration constant. */
    static final String MAX_REQUEST_SIZE = "MAX_REQUEST_SIZE";

    /** The read buffer size configuration constant. */
    static final String READ_BUFFER_SIZE = "READ_BUFFER_SIZE";

    /** The request timeout configuration constant. */
    static final String REQUEST_TIMEOUT = "REQUEST_TIMEOUT";

    /**
     * Creates a new configuration constants class.
     */
    private Config() {
        // This is intentionally left empty.
    }
}
